# 🚀 БЫСТРЫЙ СТАРТ

## Шаг 1: Установка зависимостей

Откройте PowerShell в папке проекта и выполните:

```powershell
pip install -r requirements.txt
```

## Шаг 2: Получение токена бота

1. Перейдите на https://discord.com/developers/applications
2. Нажмите "New Application" и создайте приложение
3. Перейдите в "Bot" → "Add Bot"
4. Скопируйте токен (нажмите "Copy")

## Шаг 3: Вставьте токен в код

Откройте файл **main.py** и замените:
```python
TOKEN = "YOUR_BOT_TOKEN_HERE"
```
на:
```python
TOKEN = "ВАШ_ТОКЕН_ЗДЕСЬ"
```

Пример:
```python
TOKEN = "MTQ1MjU4OTIyODg3Njg5MDExMg.Gn2pBx.2-HfFzUls3SmM8yYBnQH7UsflYkH1evPEF4ay8"
```

## Шаг 4: Добавьте бота на сервер

1. В Developer Portal откройте "OAuth2" → "URL Generator"
2. Выберите scope: `bot`
3. Выберите permissions:
   - ✅ Ban Members
   - ✅ Kick Members
   - ✅ Manage Messages
   - ✅ Manage Roles
4. Скопируйте сгенерированный URL и откройте его в браузере
5. Выберите ваш тестовый сервер и подтвердите

## Шаг 5: Обновите ID сервера (если нужно)

Откройте **main.py** и измените:
```python
test_guilds=[1394857805118181407]  # ← Замените на ID вашего сервера
```

Как получить ID сервера:
- Включите Developer Mode в Discord (Settings → Advanced → Developer Mode)
- Кликните правой кнопкой на сервер → Copy Server ID

## Шаг 6: Запустите бота

```powershell
python main.py
```

Если все правильно, вы увидите:
```
✅ Бот ВАШЕ_ИМЯ#1234 запущен!
✓ Загружен: ban.py
✓ Загружен: clear.py
✓ Загружен: giverole.py
✓ Загружен: help.py
```

## 📝 Команды бота

Напишите `/help` в любом канале сервера для справки.

### Основные команды:
- `/ban` - Забанить пользователя
- `/unban` - Разбанить пользователя
- `/kick` - Кикнуть пользователя
- `/temp_ban` - Временный бан (указать duration и unit)
- `/clear` - Очистить сообщения (max 100)
- `/give_role` - Выдать роль
- `/remove_role` - Удалить роль
- `/help` - Справка

## 🎵 Для работы музыки: установите ffmpeg

Музыкальный плеер требует `ffmpeg`:

### Windows:
1. Скачайте ffmpeg с https://www.gyan.dev/ffmpeg/builds/ (выберите "Full Build")
2. Распакуйте архив в папку, например `C:\ffmpeg`
3. Добавьте путь в системную переменную `PATH`:
   - Settings → System → About → Advanced system settings → Environment Variables
   - Отредактируйте `Path` и добавьте: `C:\ffmpeg\bin`
4. Проверьте в PowerShell:
   ```powershell
   ffmpeg -version
   ```

### Linux / macOS
```bash
# Ubuntu/Debian
sudo apt-get install ffmpeg

# macOS (Homebrew)
brew install ffmpeg
```

После установки ffmpeg команды музыки (`!play`, `!join`, `!skip` и т.д.) будут работать.

## ✅ Исправленные ошибки

✅ Все опечатки исправлены (intentation → interaction)
✅ Все методы исправлены (mentlon → mention, gulld → guild)
✅ Добавлена обработка ошибок
✅ Добавлены проверки прав доступа
✅ Все команды работают через Slash Commands и текстовые команды (!)
✅ Улучшено форматирование сообщений (эмодзи + статус)
✅ Добавлены команды мьютирования и музыки
✅ yt-dlp и PyNaCl установлены для музыкального плеера

## 🆘 Если что-то не работает

1. Убедитесь, что бот имеет права администратора на сервере
2. Проверьте, что токен правильно скопирован (без пробелов)
3. Проверьте, что ID сервера совпадает (test_guilds)
4. Перезагрузите сервер Discord (выйдите и войдите)
5. Проверьте консоль на наличие ошибок
